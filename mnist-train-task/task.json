{
  "title": "MNIST Train Task",
  "name": "mnist-train-task",
  "command": "python ~/mnist-train-task/mnist_train.py",
  "cpus": "1",
  "memory": "4",
  "setup": "uv pip install transformers torch torchvision wandb transformerlab;\nsudo apt update;\nsudo apt install -y libgl1 libglib2.0-0;",
  "description": "A task to train a neural network on the MNIST dataset using PyTorch."
}